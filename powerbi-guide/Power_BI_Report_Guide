# Power BI Report Development Guide

## Army Unit Readiness Analytics Dashboard

---

## **Overview**

This guide provides detailed instructions for building a comprehensive Power BI report using the Army Readiness semantic model. The report will consist of multiple pages covering personnel, equipment, and training analytics.

**Report Pages**:

1. Executive Dashboard (Overview KPIs)
2. Personnel Readiness Detail
3. Equipment Readiness Detail
4. Training Metrics
5. Integrated Analytics

---

## **Prerequisites**

- Semantic model `ArmyReadinessSemanticModel` created and published
- Power BI Desktop or Power BI Service access
- Familiarity with Power BI visuals and DAX

---

## **Part 1: DAX Measures Library**

Before building visuals, create these calculated measures in your semantic model.

### **1.1 Personnel Readiness Measures**

**Open the semantic model** and navigate to the **Model view**. Select the appropriate table and add these measures:

**In `gold_unit_readiness_summary` table:**

```dax
// Total number of unique units
Total Units = DISTINCTCOUNT(gold_unit_readiness_summary[UnitID])
```

```dax
// Count of units with GREEN status
Units GREEN = 
CALCULATE(
    [Total Units],
    gold_unit_readiness_summary[OverallReadiness] = "GREEN"
)
```

```dax
// Count of units with AMBER status
Units AMBER = 
CALCULATE(
    [Total Units],
    gold_unit_readiness_summary[OverallReadiness] = "AMBER"
)
```

```dax
// Count of units with RED status
Units RED = 
CALCULATE(
    [Total Units],
    gold_unit_readiness_summary[OverallReadiness] = "RED"
)
```

```dax
// Overall readiness percentage (GREEN units / Total units)
Readiness % = 
DIVIDE(
    [Units GREEN],
    [Total Units],
    0
) * 100
```

```dax
// Average readiness score across all units
Avg Readiness Score = 
AVERAGE(gold_unit_readiness_summary[AvgReadinessScore])
```

```dax
// Total assigned personnel across all units
Total Personnel Assigned = 
SUM(silver_personnel_readiness[PersonnelAssigned])
```

```dax
// Total present personnel
Total Personnel Present = 
SUM(silver_personnel_readiness[PersonnelPresent])
```

```dax
// Personnel presence percentage
Personnel Present % = 
DIVIDE(
    [Total Personnel Present],
    [Total Personnel Assigned],
    0
) * 100
```

### **1.2 Equipment Readiness Measures**

**In `silver_equipment_inventory` table:**

```dax
// Total equipment items
Total Equipment = 
SUM(silver_equipment_inventory[Quantity])
```

```dax
// Total operational equipment
Operational Equipment = 
SUM(silver_equipment_inventory[OperationalQty])
```

```dax
// Equipment in maintenance
Equipment in Maintenance = 
SUM(silver_equipment_inventory[MaintenanceQty])
```

```dax
// Non-operational equipment
Non-Operational Equipment = 
SUM(silver_equipment_inventory[NonOperationalQty])
```

```dax
// Equipment readiness percentage
Equipment Readiness % = 
DIVIDE(
    [Operational Equipment],
    [Total Equipment],
    0
) * 100
```

```dax
// Equipment needing attention (condition status)
Equipment Needs Attention = 
CALCULATE(
    [Total Equipment],
    silver_equipment_inventory[ConditionStatus] = "NEEDS_ATTENTION"
)
```

```dax
// Equipment with overdue inspections
Equipment Overdue Inspection = 
CALCULATE(
    COUNTROWS(silver_equipment_inventory),
    silver_equipment_inventory[InspectionStatus] = "Overdue"
)
```

### **1.3 Training Measures**

**In `silver_training_metrics` table:**

```dax
// Total training events
Total Training Events = 
COUNTROWS(silver_training_metrics)
```

```dax
// Completed training events
Completed Training Events = 
CALCULATE(
    [Total Training Events],
    silver_training_metrics[TrainingStatus] = "COMPLETED"
)
```

```dax
// Average training pass rate
Avg Training Pass Rate = 
AVERAGE(silver_training_metrics[PassRate])
```

```dax
// Average training completion rate
Avg Completion Rate = 
AVERAGE(silver_training_metrics[CompletionRate])
```

```dax
// Total participants trained
Total Participants Trained = 
SUM(silver_training_metrics[ParticipantsCompleted])
```

```dax
// Highly effective training events
Highly Effective Training = 
CALCULATE(
    [Total Training Events],
    silver_training_metrics[TrainingEffectiveness] = "Highly Effective"
)
```

### **1.4 Advanced Calculated Measures**

**In `gold_unit_readiness_summary` table:**

```dax
// Readiness trend indicator (MoM change)
Readiness Trend = 
VAR CurrentAvg = [Avg Readiness Score]
VAR PreviousAvg = 
    CALCULATE(
        [Avg Readiness Score],
        DATEADD(silver_personnel_readiness[ReportDate], -1, MONTH)
    )
RETURN
    IF(
        ISBLANK(PreviousAvg),
        BLANK(),
        CurrentAvg - PreviousAvg
    )
```

```dax
// Readiness status icon (for conditional formatting)
Readiness Status Icon = 
SWITCH(
    TRUE(),
    [Readiness %] >= 95, "🟢 Excellent",
    [Readiness %] >= 90, "🟡 Good",
    [Readiness %] >= 85, "🟠 Satisfactory",
    "🔴 Needs Attention"
)
```

```dax
// Combined readiness score (weighted: 50% personnel, 30% equipment, 20% training)
Combined Readiness Score = 
VAR PersonnelScore = [Avg Readiness Score]
VAR EquipmentScore = [Equipment Readiness %]
VAR TrainingScore = [Avg Training Pass Rate]
RETURN
    (PersonnelScore * 0.5) + (EquipmentScore * 0.3) + (TrainingScore * 0.2)
```

```dax
// Units at risk (readiness < 85)
Units at Risk = 
CALCULATE(
    [Total Units],
    gold_unit_readiness_summary[AvgReadinessScore] < 85
)
```

---

## **Part 2: Report Page 1 - Executive Dashboard**

### **2.1 Page Setup**

1. Create a new report from your semantic model
2. Rename the page to: **"Executive Dashboard"**
3. Set page size to **16:9** (View → Page Size → 16:9)
4. Apply background color: **#F5F5F5** (light gray)

### **2.2 Add Header Section**

1. **Insert → Text box**
  
2. Text: **"ARMY UNIT READINESS DASHBOARD"**
  
3. Formatting:
  
  - Font: **Segoe UI Bold**
  - Size: **28 pt**
  - Color: **White (#FFFFFF)**
  - Alignment: **Center**
4. **Insert → Shapes → Rectangle** behind text
  
5. Rectangle formatting:
  
  - Fill: **Dark Blue (#003366)**
  - Height: **80 pixels**
  - Position: Top of page, full width
6. **Add subtitle text box**:
  
  - Text: **"Real-Time Unit Readiness Analytics"**
  - Font: **Segoe UI Semibold, 14pt, White**
  - Position below main title

### **2.3 Add Key Performance Indicator Cards**

Create **5 KPI cards** across the top (below header):

**Card 1: Total Units**

1. Insert → Visualizations → **Card**
2. Field: `[Total Units]`
3. Formatting:
  - Callout value font size: **48pt**
  - Category label: **"Total Units"**
  - Category label font size: **14pt**
  - Background: **White**
  - Border: **Light gray, 2px**

**Card 2: Readiness Percentage**

1. Insert → **Card**
2. Field: `[Readiness %]`
3. Formatting:
  - Display units: **None**
  - Decimal places: **1**
  - Add **% symbol**
  - Conditional formatting:
    - Values >= 95: **Green (#00B050)**
    - Values 85-94: **Amber (#FFC000)**
    - Values < 85: **Red (#FF0000)**

**Card 3: Equipment Readiness**

1. Insert → **Card**
2. Field: `[Equipment Readiness %]`
3. Formatting similar to Card 2

**Card 4: Avg Training Pass Rate**

1. Insert → **Card**
2. Field: `[Avg Training Pass Rate]`
3. Formatting similar to Card 2

**Card 5: Personnel Present %**

1. Insert → **Card**
2. Field: `[Personnel Present %]`
3. Formatting similar to Card 2

### **2.4 Add Status Distribution Visual**

**Visual: Readiness Status by Division (Stacked Bar Chart)**

1. Insert → **Stacked bar chart**
2. **Y-axis**: `gold_unit_readiness_summary[Division]`
3. **X-axis**: `[Total Units]`
4. **Legend**: `gold_unit_readiness_summary[OverallReadiness]`
5. **Formatting**:
  - Title: **"Unit Readiness Status by Division"**
  - Data colors:
    - GREEN → **#00B050**
    - AMBER → **#FFC000**
    - RED → **#FF0000**
  - Data labels: **On**
  - X-axis title: **"Number of Units"**
  - Y-axis title: **"Division"**

**Position**: Left side, middle section

### **2.5 Add Readiness Trend Chart**

**Visual: Readiness Score Trend (Line Chart)**

1. Insert → **Line chart**
2. **X-axis**: `silver_personnel_readiness[ReportDate]` (Date hierarchy: Month)
3. **Y-axis**: `silver_personnel_readiness[ReadinessScore]` (Average)
4. **Legend**: `silver_personnel_readiness[Brigade]`
5. **Formatting**:
  - Title: **"Readiness Score Trend by Brigade"**
  - Line styles: Different colors per brigade
  - Markers: **On**
  - Data labels: **Off**
  - Y-axis range: **75 to 100**

**Position**: Right side, middle section

### **2.6 Add Equipment Status Donut Chart**

**Visual: Equipment Condition Status (Donut Chart)**

1. Insert → **Donut chart**
2. **Legend**: `silver_equipment_inventory[ConditionStatus]`
3. **Values**: `[Total Equipment]`
4. **Formatting**:
  - Title: **"Equipment Condition Status"**
  - Data colors:
    - SERVICEABLE → **Green**
    - NEEDS_ATTENTION → **Orange**
  - Detail labels: **Category, percentage**

**Position**: Bottom left

### **2.7 Add Training Effectiveness Chart**

**Visual: Training Pass Rates by Type (Clustered Column)**

1. Insert → **Clustered column chart**
2. **X-axis**: `silver_training_metrics[TrainingType]`
3. **Y-axis**: `[Avg Training Pass Rate]`
4. **Formatting**:
  - Title: **"Training Pass Rates by Type"**
  - Data labels: **On**
  - Y-axis range: **0 to 100**
  - Target line at **90** (good performance threshold)

**Position**: Bottom center

### **2.8 Add Top/Bottom Units Table**

**Visual: Top & Bottom Performing Units (Table)**

1. Insert → **Table**
2. **Columns**:
  - `gold_unit_readiness_summary[UnitName]`
  - `gold_unit_readiness_summary[Division]`
  - `gold_unit_readiness_summary[AvgReadinessScore]`
  - `gold_unit_readiness_summary[OverallReadiness]`
3. **Formatting**:
  - Title: **"Unit Performance Overview"**
  - Conditional formatting on `OverallReadiness`:
    - GREEN → Green background
    - AMBER → Amber background
    - RED → Red background
  - Sort by: `AvgReadinessScore` descending
  - Show top 10 rows

**Position**: Bottom right

### **2.9 Add Interactive Slicers**

**Slicer 1: Division Filter**

1. Insert → **Slicer**
2. Field: `silver_personnel_readiness[Division]`
3. Style: **Dropdown** or **Tile**
4. Position: **Top right corner**

**Slicer 2: Date Range**

1. Insert → **Slicer**
2. Field: `silver_personnel_readiness[ReportDate]`
3. Style: **Between** (date range slider)
4. Position: **Below division slicer**

---

## **Part 3: Report Page 2 - Personnel Readiness Detail**

### **3.1 Page Setup**

1. Add new page
2. Rename: **"Personnel Readiness"**
3. Copy header from Page 1 (Ctrl+C, Ctrl+V)
4. Update subtitle to: **"Personnel Manning and Readiness Analysis"**

### **3.2 Add Personnel Summary Cards**

**Create 4 cards:**

1. **Total Personnel Assigned**: `[Total Personnel Assigned]`
2. **Total Personnel Present**: `[Total Personnel Present]`
3. **Personnel Absent**: `[Total Personnel Assigned] - [Total Personnel Present]` (new measure)
4. **Personnel Present %**: `[Personnel Present %]`

### **3.3 Add Personnel Detail Table**

**Visual: Personnel Readiness by Unit (Table)**

1. Insert → **Table**
2. **Columns**:
  - `silver_personnel_readiness[UnitName]`
  - `silver_personnel_readiness[Brigade]`
  - `silver_personnel_readiness[Division]`
  - `silver_personnel_readiness[PersonnelAssigned]`
  - `silver_personnel_readiness[PersonnelPresent]`
  - `silver_personnel_readiness[PersonnelPresentPct]`
  - `silver_personnel_readiness[ReadinessScore]`
  - `silver_personnel_readiness[ReadinessStatus]`
3. **Conditional formatting**:
  - `ReadinessStatus`:
    - GREEN → Green cell background
    - AMBER → Amber cell background
    - RED → Red cell background
  - `ReadinessScore`: Data bars (green gradient)
4. Enable **search** and **sorting**

### **3.4 Add Personnel Present % by Unit Chart**

**Visual: Personnel Present % (Bar Chart)**

1. Insert → **Clustered bar chart**
2. **Y-axis**: `silver_personnel_readiness[UnitName]`
3. **X-axis**: `silver_personnel_readiness[PersonnelPresentPct]` (Average)
4. **Data labels**: **On**
5. **Conditional formatting**: Color by value (red to green gradient)
6. Sort by: `PersonnelPresentPct` descending

### **3.5 Add Readiness Category Distribution**

**Visual: Readiness Category Breakdown (Pie Chart)**

1. Insert → **Pie chart**
2. **Legend**: `silver_personnel_readiness[ReadinessCategory]`
3. **Values**: Count of `UnitID`
4. **Labels**: Category + percentage

### **3.6 Add Trend Analysis**

**Visual: Personnel Readiness Over Time (Area Chart)**

1. Insert → **Area chart**
2. **X-axis**: `silver_personnel_readiness[ReportDate]`
3. **Y-axis**: `silver_personnel_readiness[ReadinessScore]` (Average)
4. **Legend**: `silver_personnel_readiness[Division]`
5. Enable **forecast** for 1 month ahead

---

## **Part 4: Report Page 3 - Equipment Readiness Detail**

### **4.1 Page Setup**

1. Add new page
2. Rename: **"Equipment Readiness"**
3. Copy header and update subtitle: **"Equipment Condition and Maintenance Status"**

### **4.2 Add Equipment Summary Cards**

**Create 5 cards:**

1. **Total Equipment**: `[Total Equipment]`
2. **Operational Equipment**: `[Operational Equipment]`
3. **Equipment Readiness %**: `[Equipment Readiness %]`
4. **Equipment in Maintenance**: `[Equipment in Maintenance]`
5. **Overdue Inspections**: `[Equipment Overdue Inspection]`

### **4.3 Add Equipment Matrix**

**Visual: Equipment by Type and Status (Matrix)**

1. Insert → **Matrix**
2. **Rows**: `silver_equipment_inventory[EquipmentType]`
3. **Columns**: `silver_equipment_inventory[ConditionStatus]`
4. **Values**: `[Total Equipment]`
5. **Conditional formatting**: Heat map (red to green)
6. Enable **drill-down** by `EquipmentName`

### **4.4 Add Equipment Detail Table**

**Visual: Equipment Inventory Detail (Table)**

1. Insert → **Table**
2. **Columns**:
  - `vw_Equipment_Summary[UnitName]` (if view created, otherwise join)
  - `silver_equipment_inventory[EquipmentType]`
  - `silver_equipment_inventory[EquipmentName]`
  - `silver_equipment_inventory[Quantity]`
  - `silver_equipment_inventory[OperationalQty]`
  - `silver_equipment_inventory[OperationalPct]`
  - `silver_equipment_inventory[ConditionStatus]`
  - `silver_equipment_inventory[InspectionStatus]`
3. **Conditional formatting** on `ConditionStatus` and `InspectionStatus`

### **4.5 Add Operational % by Equipment Type**

**Visual: Equipment Readiness by Type (Clustered Column)**

1. Insert → **Clustered column chart**
2. **X-axis**: `silver_equipment_inventory[EquipmentType]`
3. **Y-axis**: `[Equipment Readiness %]`
4. **Data labels**: On
5. **Target line**: 90% (threshold)

### **4.6 Add Inspection Status Chart**

**Visual: Inspection Status Distribution (Stacked Column)**

1. Insert → **Stacked column chart**
2. **X-axis**: `silver_equipment_inventory[EquipmentType]`
3. **Y-axis**: Count of `EquipmentID`
4. **Legend**: `silver_equipment_inventory[InspectionStatus]`
5. **Colors**:
  - Overdue → Red
  - Due Soon → Yellow
  - Current → Green

### **4.7 Add Equipment Gauges (Optional)**

For each major equipment type (Vehicle, Weapon, Communications):

1. Insert → **Gauge**
2. **Value**: `[Equipment Readiness %]`
3. **Filter**: By specific `EquipmentType`
4. **Range**: 0-100, Target: 90
5. Arrange 3 gauges horizontally

---

## **Part 5: Report Page 4 - Training Metrics**

### **5.1 Page Setup**

1. Add new page
2. Rename: **"Training Metrics"**
3. Copy header and update subtitle: **"Training Completion and Effectiveness Analysis"**

### **5.2 Add Training Summary Cards**

**Create 4 cards:**

1. **Total Training Events**: `[Total Training Events]`
2. **Completed Events**: `[Completed Training Events]`
3. **Avg Pass Rate**: `[Avg Training Pass Rate]`
4. **Total Participants**: `[Total Participants Trained]`

### **5.3 Add Training Completion Chart**

**Visual: Training Completion by Unit (Clustered Bar)**

1. Insert → **Clustered bar chart**
2. **Y-axis**: `silver_training_metrics[UnitID]` or join to get `UnitName`
3. **X-axis**:
  - `silver_training_metrics[ParticipantsScheduled]` (Sum)
  - `silver_training_metrics[ParticipantsCompleted]` (Sum)
4. **Data labels**: On
5. Sort by completion rate

### **5.4 Add Training Detail Table**

**Visual: Training Events Detail (Table)**

1. Insert → **Table**
2. **Columns**:
  - `silver_training_metrics[UnitID]`
  - `silver_training_metrics[TrainingType]`
  - `silver_training_metrics[TrainingName]`
  - `silver_training_metrics[CompletionRate]`
  - `silver_training_metrics[PassRate]`
  - `silver_training_metrics[AverageScore]`
  - `silver_training_metrics[TrainingEffectiveness]`
3. **Conditional formatting** on `TrainingEffectiveness`

### **5.5 Add Effectiveness Funnel**

**Visual: Training Effectiveness Distribution (Funnel)**

1. Insert → **Funnel chart**
2. **Category**: `silver_training_metrics[TrainingEffectiveness]`
3. **Values**: Count of `TrainingID`
4. **Order**: Highly Effective → Effective → Satisfactory → Needs Improvement

### **5.6 Add Scatter Plot**

**Visual: Pass Rate vs Completion Rate (Scatter Chart)**

1. Insert → **Scatter chart**
2. **X-axis**: `silver_training_metrics[CompletionRate]`
3. **Y-axis**: `silver_training_metrics[PassRate]`
4. **Legend**: `silver_training_metrics[TrainingType]`
5. **Size**: `silver_training_metrics[ParticipantsCompleted]` (Sum)
6. **Play axis**: `silver_training_metrics[CompletedDate]` (Optional for animation)

---

## **Part 6: Report Page 5 - Integrated Analytics**

### **6.1 Page Setup**

1. Add new page
2. Rename: **"Integrated Analytics"**
3. Subtitle: **"Cross-Domain Readiness Insights"**

### **6.2 Add Combined Readiness Score Card**

**Card: Combined Readiness Score**

- Field: `[Combined Readiness Score]` (created earlier)
- Large callout value

### **6.3 Add Multi-Metric Table**

**Visual: Comprehensive Unit Dashboard (Table)**

1. Insert → **Table**
2. **Columns**:
  - Unit Name (from gold summary)
  - Division
  - Personnel Readiness (`AvgReadinessScore`)
  - Equipment Readiness (calculate avg by unit)
  - Training Pass Rate (calculate avg by unit)
  - `[Combined Readiness Score]`
  - Status (icon based on score)
3. **Conditional formatting**: Heat map on all score columns

### **6.4 Add Correlation Chart**

**Visual: Personnel vs Equipment Readiness (Scatter)**

1. Insert → **Scatter chart**
2. **X-axis**: Personnel readiness score
3. **Y-axis**: Equipment readiness %
4. **Legend**: Division
5. **Size**: Total personnel
6. Add **trend line**

### **6.5 Add Risk Matrix**

**Visual: Units at Risk (Table filtered)**

1. Insert → **Table**
2. Show units where:
  - Personnel readiness < 85 OR
  - Equipment readiness < 80 OR
  - Training pass rate < 85
3. Highlight with red background

---

## **Part 7: Report Enhancements**

### **7.1 Add Navigation**

1. Insert → **Button** → **Navigator** → **Page navigator**
2. Position on each page (consistent location)
3. Format with professional styling

### **7.2 Add Bookmarks**

Create bookmarks for common filter states:

- "All Units"
- "1st Infantry Division Only"
- "82nd Airborne Only"
- "Units at Risk"

### **7.3 Add Drill-Through Pages**

Create a drill-through page for **Unit Detail**:

1. Add new page: "Unit Detail Drill-Through"
2. Add drill-through filter: `UnitID`
3. Add comprehensive visuals for single unit:
  - Personnel trends
  - Equipment inventory
  - Training history
4. Enable drill-through from other pages

### **7.4 Apply Consistent Theme**

1. View → Themes → **Executive** or create custom
2. Ensure all pages use same color palette
3. Consistent font sizes and styles
4. Aligned visual positions

### **7.5 Add Report-Level Filters**

In the **Filters pane**, add:

- `Division` (multi-select)
- `ReportDate` (date range)
- `OverallReadiness` (status filter)

---

## **Part 8: Testing and Validation**

### **8.1 Data Accuracy Checks**

- Verify KPIs match SQL query results
- Test drill-through functionality
- Confirm filters apply correctly across pages

### **8.2 Performance Testing**

- Check report load time
- Optimize visuals if slow (reduce data points, use aggregations)
- Consider using aggregation tables for large datasets

### **8.3 Mobile Layout**

1. View → **Mobile layout**
2. Rearrange visuals for mobile viewing
3. Prioritize key metrics for small screens

---

## **Part 9: Publishing and Sharing**

### **9.1 Publish Report**

1. File → **Publish** → **Publish to Power BI**
2. Select workspace: `Army_Readiness_Analytics`
3. Wait for successful publication

### **9.2 Configure Scheduled Refresh**

1. In Power BI Service, go to semantic model settings
2. **Scheduled refresh** → Configure:
  - Frequency: **Daily**
  - Time: **07:00** (after pipeline and notebook run)
3. Test refresh

### **9.3 Set Permissions**

1. Share report with appropriate users
2. Configure **Row-Level Security** (RLS) if needed:
  - Users see only their division's data
  - Implement in semantic model, test in service

### **9.4 Create Dashboard (Optional)**

1. From report, **Pin** key visuals to new dashboard
2. Create "Army Readiness Executive Dashboard"
3. Arrange tiles logically
4. Add live Q&A tile
5. Share dashboard URL with leadership

---

## **DAX Quick Reference**

### **Common Patterns:**

**Calculate with Filter:**

```dax
Measure = 
CALCULATE(
    SUM(Table[Column]),
    Table[FilterColumn] = "Value"
)
```

**Previous Period Comparison:**

```dax
Previous Month = 
CALCULATE(
    [Measure],
    DATEADD(DateTable[Date], -1, MONTH)
)
```

**Percentage Calculation:**

```dax
Percentage = 
DIVIDE(
    [Numerator],
    [Denominator],
    0
) * 100
```

**Conditional Measure:**

```dax
Status = 
SWITCH(
    TRUE(),
    [Score] >= 90, "Good",
    [Score] >= 80, "Fair",
    "Poor"
)
```

---

## **Formatting Best Practices**

### **Colors:**

- Use military standard colors (green/amber/red for status)
- Maintain 3-4 color limit per visual
- Ensure accessibility (colorblind-friendly palettes)

### **Fonts:**

- Title: **16-20pt, Bold**
- Data labels: **10-12pt**
- Card values: **36-48pt, Bold**

### **Layout:**

- Align visuals to grid
- Consistent padding (10-15px)
- Group related visuals
- Use white space effectively

---

## **Troubleshooting**

**Issue: Visuals not showing data**

- Check filters and slicers
- Verify relationships in model
- Ensure data refresh completed

**Issue: Slow report performance**

- Reduce number of visuals per page
- Use aggregated measures instead of calculated columns
- Optimize DAX (avoid row-by-row calculations)

**Issue: Incorrect calculations**

- Verify filter context in DAX
- Check relationship cardinality
- Use DAX Studio to debug measures

---

## **Next Steps**

1. ✅ Build all 5 report pages
2. ✅ Test thoroughly with sample data
3. ✅ Gather feedback from stakeholders
4. ✅ Iterate and refine based on feedback
5. ✅ Deploy to production
6. ✅ Train end users
7. ✅ Schedule regular reviews and updates

---

**UNCLASSIFIED // FOR TRAINING USE ONLY**

---

**Document Version**: 1.0  
**Last Updated**: November 2025  
**Contact**: Microsoft Fabric Lab Team
